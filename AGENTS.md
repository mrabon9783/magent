Goal: Amarr station-trading radar (ESI polling + watchlist + opportunities + alerts)

Constraints: No in-client automation, only data + recommendations

Tech: .NET 8, SQLite, Docker-friendly, config via JSON

Quality gates: dotnet format (optional), dotnet test, basic linting, CI if you want

Deliverables: CLI commands, schema migrations, sample config, README, and a simple HTML/MD report
